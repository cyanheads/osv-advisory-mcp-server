---
summary: "Adopt mcp-ts-core ^0.10.9; vendored devcheck scripts gain the dependency-specifier guard and plugin-manifest packaging checks; codex-plugin longDescription filled"
breaking: false
security: false
---

# 0.1.6 — 2026-06-20

Framework-maintenance release: `@cyanheads/mcp-ts-core` `^0.10.6 → ^0.10.9`, with the devcheck and packaging guards that ship across 0.10.7–0.10.9. No source or behavior changes.

## Added

- **`check-dependency-specifiers` devcheck step** — vendored `scripts/check-dependency-specifiers.ts`, registered in devcheck as `Dependency Specifiers`; hard-fails on floating specifiers (`latest`, `*`, pre-release dist-tags) in `package.json`'s dependency sections and `bun.lock`'s `workspaces` map. Catches a `bun update --latest` writing a literal `latest` dist-tag into the lockfile. (cyanheads/mcp-ts-core#246)
- **Plugin-manifest packaging checks** — `scripts/lint-packaging.ts` validates `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`/`mcp.json` when present: non-empty descriptions, unscoped machine name in display fields, full `package.json` name in the install arg. Gated by the new `devcheck.config.json` `packaging.pluginManifests: true` flag; the devcheck Packaging gate now runs when a plugin manifest is present. (cyanheads/mcp-ts-core#240)
- **`.codex-plugin/plugin.json` `longDescription`** — filled in the previously empty field with the tool surface and chaining note.

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.10.6 → ^0.10.9` — adopts upstream devcheck and packaging changes.
- **Vendored devcheck scripts** — `build-changelog.ts`, `check-framework-antipatterns.ts`, and `devcheck.ts` skip git-dependent checks on a fresh scaffold (no `.git`); `check-skill-versions.ts` skips a `SKILL.md` deleted from the worktree instead of crashing. (cyanheads/mcp-ts-core#242, cyanheads/mcp-ts-core#243, cyanheads/mcp-ts-core#237)
- **Vendored skills synced** — re-mirrored from mcp-ts-core 0.10.7–0.10.9 to pick up propagated body drift and version bumps. (cyanheads/mcp-ts-core#238)

## Dependencies

- `@cyanheads/mcp-ts-core` `^0.10.6 → ^0.10.9`
- `@types/node` `^25.9.3 → ^26.0.0`
- `vitest` `^4.1.8 → ^4.1.9`
