---
summary: "Adopt @cyanheads/mcp-ts-core 0.9.21 — HTTP log context fix, secret-stripping, and retry improvements"
breaking: false
security: false
agent-notes: |
  Framework updated from ^0.9.16 to ^0.9.21. No API surface changes — handler code, tool definitions, and tests are unaffected. Adopt as a standard maintenance update.
---

# 0.1.3 — 2026-06-02

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.9.16 → ^0.9.21` — adopts upstream framework changes:
  - HTTP transport per-request log context fix: per-request logs and traces now carry fresh request + trace/span IDs instead of the frozen boot context.
  - `fetchWithTimeout` strips query-string secrets (e.g. `?api_key=`) from error messages and logs.
  - `withRetry` fails fast on non-retryable errors; `ctx.fail` auto-populates the `retryable` flag.

## Dependencies

- `@biomejs/biome` `^2.4.7 → ^2.4.16`
- `@types/node` `^25.6.0 → ^25.9.1`
- `tsc-alias` `^1.8.16 → ^1.8.17`
- `vitest` `^4.1.0 → ^4.1.8`
